[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "steam-achievement-translation-installer"
version = "0.13.0"
description = "Discover, import, edit, install, restore, and manage Steam achievement translations."
readme = "README.md"
requires-python = ">=3.13"
license = { file = "LICENSE" }
authors = [{ name = "GaBoron" }]
keywords = ["steam", "achievements", "localization", "translation"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "License :: OSI Approved :: MIT License",
  "Operating System :: Microsoft :: Windows :: Windows 10",
  "Operating System :: Microsoft :: Windows :: Windows 11",
  "Programming Language :: Python :: 3.13",
]
dependencies = ["dulwich==1.2.12"]

[project.optional-dependencies]
dev = ["pytest>=8.3"]

[project.scripts]
satl = "satl.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
